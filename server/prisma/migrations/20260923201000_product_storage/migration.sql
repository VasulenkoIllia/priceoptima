-- Щоденне оновлення прайсу підтверджує дату ціни в кожного товару постачальника. Із вільним місцем на сторінках
-- (fillfactor 90) такий запис лишається в тій самій сторінці й не зачіпає індекси (HOT-оновлення) — у рази менше
-- записів і журналу бази. Частіше автоприбирання — щоб таблиця на мільйон рядків не розросталась між запусками.
-- Налаштування діє на нові й перезаписані сторінки; відкат — ALTER TABLE "Product" RESET (...).
ALTER TABLE "Product" SET (
  fillfactor = 90,
  autovacuum_vacuum_scale_factor = 0.02,
  autovacuum_analyze_scale_factor = 0.02
);
