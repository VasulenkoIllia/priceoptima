-- Курс із прайсу постачальника діє N днів від дати прайсу; далі — ручний курс постачальника або загальний.
-- Додавання колонки зі значенням за замовчуванням — без переписування таблиці (один рядок налаштувань).
ALTER TABLE "AppSettings" ADD COLUMN "priceListRateMaxAgeDays" INTEGER NOT NULL DEFAULT 3;
