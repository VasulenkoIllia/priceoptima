-- Строк дії КП за замовчуванням — 3 дні, як у налаштуваннях застосунку (було 14 лише в схемі бази).
-- Змінюється тільки значення за замовчуванням; збережені налаштування не чіпаємо.
ALTER TABLE "AppSettings" ALTER COLUMN "kpValidityDays" SET DEFAULT 3;
